# Security

Do not submit private speech recordings, credentials, access tokens, personal identifiers, or proprietary datasets in issues or test fixtures.

The core package performs local deterministic reasoning over data supplied by the caller. It does not make network requests.

For security reports concerning code in this repository, use GitHub's private vulnerability reporting mechanism when enabled.
